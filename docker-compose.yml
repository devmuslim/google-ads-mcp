services:
  google-ads-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "8080"
    environment:
      PORT: "8080"
      FASTMCP_HOST: "0.0.0.0"
    restart: unless-stopped
